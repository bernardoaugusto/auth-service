version: '3.7'

services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
            target: builder
        command: sh -c "npm run start"
        ports:
            - '3000:3000'
        volumes:
            - .:/usr/app
            - /usr/src/app/node_modules
        environment:
            NODE_ENV: dev
            PORT: 3000
            MONGO_URL: mongodb://mongodb-myapp:27017/clean-node-api
        container_name: clean-node-api-app
        depends_on:
            - mongodb-myapp
    mongodb-myapp:
        container_name: mongo_container
        image: mongo:latest
        restart: always
        ports:
          - '27017:27017'
        volumes:
          - mongo_dbv:/data/db
volumes:
    mongo_dbv:
